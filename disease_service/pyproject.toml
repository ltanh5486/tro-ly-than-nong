[project]
name = "durian-disease-service"
version = "1.0.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "python-multipart",
    "numpy",
    "onnxruntime",
    "Pillow"
]

[tool.vercel]
entrypoint = "main:app"
